# logos/ — authored artwork, not generated

The one exception to "every tracked image is a build output": a conference's own logo has no
camera-ready source to regenerate it from. `test/test_assets.py` lists each file here in
`AUTHORED_IMAGES` with its origin.

| file | origin |
| --- | --- |
| `eccv-2026.svg` | `scratchpad/poster/prepared/eccv-2026-tight.svg` — the organisers' `ECCV_Color Logo_2026.svg` with its viewBox tightened to the ink (see that folder's README); no path data changed. |
